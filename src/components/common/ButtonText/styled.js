import { Button } from "antd";
import styled from 'styled-components';

export const StyledButton = styled(Button)`
    font-size: 12px;
    font-weight: 700;
    color: #1890FF;
    &.ant-btn .anticon {
        font-size: 20px;
        color: #FF8C00;
    }
`;